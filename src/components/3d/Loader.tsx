// Packages
import { FC } from 'react';

// Types
import Loader from '../../types/props/3d/loader';

const Loader: FC<Loader> = (props: Loader) => {
  // Props
  const {} = props;
  
  return (
    <div>Loader</div>
  );
};

export default Loader;
