// Packages
import { FC } from 'react';

// Types
import Scene from '../../types/props/3d/scene';

const Scene: FC<Scene> = (props: Scene) => {
  // Props
  const {} = props;
  
  return (
    <div>Scene</div>
  );
};

export default Scene;
