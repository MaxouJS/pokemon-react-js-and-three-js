// Packages
import { FC } from 'react';

// Types
import Text from '../../types/props/2d/text';

const Text: FC<Text> = (props: Text) => {
  // Props
  const {} = props;
  
  return (
    <div>Text</div>
  );
};

export default Text;
