// Packages
import { FC } from 'react';

// Types
import Card from '../../types/props/2d/card';

const Card: FC<Card> = (props: Card) => {
  // Props
  const {} = props;
  
  return (
    <div>Card</div>
  );
};

export default Card;
