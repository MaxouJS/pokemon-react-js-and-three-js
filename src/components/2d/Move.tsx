// Packages
import { FC } from 'react';

// Types
import Move from '../../types/props/2d/move';

const Move: FC<Move> = (props: Move) => {
  // Props
  const {} = props;
  
  return (
    <div>Move</div>
  );
};

export default Move;
