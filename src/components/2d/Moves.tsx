// Packages
import { FC } from 'react';

// Types
import Moves from '../../types/props/2d/moves';

const Moves: FC<Moves> = (props: Moves) => {
  // Props
  const {} = props;
  
  return (
    <div>Moves</div>
  );
};

export default Moves;
