interface Pokemon {
  pokemonName: string;
  currentLV: number;
  currentHP: number;
  maximumHP: number;
  position: number[];
  rotation: number[];
  scale: number[];
  currentAnimation: string;
};

export default Pokemon;
